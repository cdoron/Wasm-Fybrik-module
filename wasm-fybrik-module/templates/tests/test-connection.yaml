apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "transform-chart.fullname" . }}-test-connection"
  labels:
    {{- include "transform-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "transform-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
